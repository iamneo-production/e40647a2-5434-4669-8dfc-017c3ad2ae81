extends layout

block content 
    .container
      .row
        .col-lg-4
        .col-lg-4
          h2.text-center.mt-3(id = "signuppage") Create Your Own Wallet
          != messages('message', locals)
          form(action="signup/add", method="post")
            //- each account in accounts
            //-   p= account.email
            .form-group
              label(for="") Firstname:
              input.form-control(type="text",id="firstname", name="firstname" required)
            .form-group
              label(for="") Lastname:
              input.form-control(type="text",id="lastname", name="lastname" required)
            .form-group
              label(for="") E-mail:
              input.form-control(type="email",id="email", name="email" required)
            .form-group
              label(for="") Password:
              input.form-control(type="password",id="password", name="password" required)
            .form-group
              label(for="") Confirm Password:
              input.form-control(type="password",id="confirmpassword", name="con_password" required)
              span#displayErr(style="display: none;") Passwords not identical.
            .form-group
              button.btn.btn-block.btn-success(id="signupbutton") Sign Up
          a.btn.btn-block.btn-primary(href="/") Log In
          
        .col-lg-4